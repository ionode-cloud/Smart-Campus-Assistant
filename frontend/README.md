# Untitled

This is a code bundle for Untitled.

## About the Project

- This project is a frontend-only campus map web application built with React and Vite.
- All code is original and not copied from external sources.
- The project is actively under development; features and UI are subject to change.
- No backend/API integration yet—data is static and local.
- Contributions and feedback are welcome.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
